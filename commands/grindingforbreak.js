/* eslint-disable max-len */
"use strict";

module.exports = {
  number: 1,
  name: "grindingforbreak",
  description: "Describes how to reach Break Infinity.",
  execute(message) {
    message.channel.send(`Get the 10,000 cost 2x IP then save up 30,000 (60,000 on web) IP and max your crunch autobuyer interval (you'll need to complete C12 if you haven't already)`);
  }
};